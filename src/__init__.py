# This file is intentionally left empty.
#
# It serves two purposes:
# 1. It tells Python that the `src` directory is a package. This allows us to use
#    relative imports between our modules (e.g., from . import parsers).
# 2. It can be used to expose parts of your package's API at the top level,
#    but for a command-line tool, this might not be necessary.

